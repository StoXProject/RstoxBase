# Definitions used in RstoxBase:

verticalAcousticResolutionVariables <- c("Channel", "Layer", "AllLayers")

horizontalAcousticResolutionVariables <- c("EDSU", "PSU", "Stratum", "Survey")

verticalSweptAreaResolutionVariables <- c("Channel", "Layer", "AllLayers")

horizontalSweptAreaResolutionVariables <- c("Station", "PSU", "Stratum", "Survey")
